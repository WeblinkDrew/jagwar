---
scope: Jagwar business workflows as additive capabilities on pinned Onlook baseline 423e2e924366419e418ee049093872d535eea41a
purpose: Implementation-agent and reviewer build substrate before large implementation
altitude: initiative
updated: 2026-07-28T13:20
---

- (constraint) Pinned baseline 423e2e924366419e418ee049093872d535eea41a is the architecture boundary; every file present there is protected and no such file may change without one exact Core Change Request and Andrew's confirmation.
- (constraint) Jagwar extends Onlook and preserves Supabase Auth, user_projects project membership, projects/branches/editor, AI prompts/tools/apply behavior, CodeSandbox, deployments/Freestyle/domains, Stripe billing, @onlook/ui, Bun workspaces, and every existing capability.
- (constraint) Jagwar is the only current product name; Telio is permitted only for exact donor or migration provenance and cannot become a new target namespace.
- (decision) Use an extension-first modular monolith: route-local vertical slices in apps/web/client, focused capability packages with public entry points, Drizzle/Postgres persistence, and ports-and-adapters only at provider edges. This binds all Jagwar units and prevents a donor-shaped parallel application.
- (decision) Map the handoff ownershipScopeId to the authenticated Supabase user ID for the first release. Every Jagwar business row is user-owned; browser owner IDs are ignored. Project links additionally require user_projects membership. No Workspace or organization aggregate is introduced.
- (decision) Keep persistence in @onlook/db Drizzle schemas and Supabase PostgreSQL. Server operations authenticate through protected tRPC or an authenticated internal worker boundary, apply resource authorization explicitly, and add RLS as defense in depth; RLS alone cannot authorize direct server Drizzle traffic.
- (decision) Create focused packages @onlook/leads, @onlook/business-policy, @onlook/durable-operation, @onlook/outreach, and @onlook/activation. Each owns only its named schemas and pure rules; provider SDKs, DB clients, React, editor stores, Stripe, and secrets stay outside.
- (decision) Versioned cross-domain policy authority lives in @onlook/business-policy plus additive policy_release persistence. It composes closed qualification, outreach, activation, and retention policy schemas; secrets and arbitrary JSON, SQL, code, or provider payloads are forbidden.
- (decision) Durable operation state-machine and idempotency contracts live in @onlook/durable-operation; durable rows and attempts live in additive @onlook/db operation schema. Queue payloads contain only operation ID, kind, version, and trace ID, never secrets, raw provider payloads, or business snapshots.
- (decision) Select logged Supabase Queues built on PGMQ as the single durable transport and Supabase Cron as the trigger for a bounded authenticated Next.js internal consumer. The consumer synchronously claims and processes a bounded batch; it never uses waitUntil or route-lifetime detached promises.
- (decision) The operation row, not PGMQ, is domain truth. Admission writes the operation and pgmq.send in one PostgreSQL transaction; visibility expiry permits recovery; attempts lease with a fencing token; success archives the message; retry uses bounded backoff; terminal exhaustion creates operator-visible dead-letter state.
- (decision) External effects remain at-least-once attempts guarded by stable provider idempotency and reconciliation. Cancellation may commit only before an atomic dispatch-start marker; after dispatch begins the result must be reconciled and cannot be relabeled canceled.
- (decision) @onlook/leads owns CandidateV1 normalization, explicit-null facts, provenance, dedupe-key versioning, qualification evidence, opportunity ranking, discovery provider ports, and pure contract tests. Route-local /leads UI and web server application services own orchestration and never persist raw provider payloads as facts.
- (decision) Prospect projects remain canonical Onlook projects. A Project Link stores only lead-to-project identity and lifecycle, requires current user_projects access, and becomes active only after the native creation flow completes and personalized-draft evidence passes.
- (decision) JagwarBusinessContextV1 is a new validated read-only contract with verified facts/provenance, explicit unknowns, rights-cleared asset references, and separate generated guidance. A deterministic renderer serializes it into existing CreateRequestContextType.PROMPT, which flows through project_create_requests, useStartProject, and ChatType.CREATE. No MessageContext type, AI prompt, agent, tool, registry, manager, stream, or apply path changes.
- (decision) A Publication Reference is an immutable Jagwar snapshot that points to an authorized completed Onlook deployment and exact public URL/version/timestamps. It never replaces deployments or domain authority, and past sends never follow a later republish.
- (decision) Onlook products, prices, subscriptions, rate_limits, usage_records, @onlook/stripe, and Stripe webhook remain the sole commercial authority. Pre-commercial cost observations are non-enforcing operational telemetry and cannot grant entitlement, decrement allowance, charge, or drive checkout before OD-14 approval.
- (decision) All customer operations enter through server-authoritative tRPC procedures; all provider work executes through adapters from application services or the durable consumer. UI renders operation projections, uses @onlook/ui and next-intl, and never owns consent, scope, entitlement, usage, compliance, send, or project decisions.
- (decision) Each business mutation carries user ID from session, stable idempotency key, trace ID, aggregate ID and version, UTC timestamp, and typed safe error. Immutable evidence is append-only; projections are rebuildable; unknown data never becomes a guessed fact.
- (decision) Preserve current application deployment, CodeSandbox, Freestyle publication, and custom-domain topology. Initial work runs only with deterministic fakes or approved non-production providers; production sends, billing mutation, customer cutover, destructive migration, and infrastructure replacement require separate approved runbooks.
- (constraint) OD-15 remains an external source-access blocker: apps/admin at 3dd1caaab9137203156e59fd48a72d0ef82b942d is unavailable, so frozen installation and complete baseline regression claims are impossible. No workspace or lockfile workaround is authorized.
- (assumption) Supabase Queues and Cron are available in the deployed target; the pinned local Supabase CLI and database image have not been started because dependency bootstrap is blocked. Implementation must record SELECT extversion for pgmq and pg_cron before Story 1.4.
- (version) Pinned stack seed: Bun 1.3.1; Next.js 16.0.7; React 19.2.0; tRPC 11.6.0 locked; Drizzle ORM 0.44.7 locked; Zod 4.1.12 locked; Supabase CLI 2.53.6 locked; AI SDK 5.0.26 web and 5.0.60 packages; Stripe SDK 18.5.0 locked; Freestyle 0.0.78.
- (version) Current upstream queue evidence checked 2026-07-28: PGMQ 1.11.1 and pg_cron 1.6.7; Supabase documents PGMQ availability from Postgres 15.6.1.143. The target-provided extension versions, not upstream latest, must be pinned by preflight.
- (question) Blocker: obtain legitimate pinned apps/admin source or an upstream-authorized resolution before dependency install and full baseline verification.
- (question) Preflight gate: if the pinned local/hosted Supabase target cannot provide logged pgmq plus pg_cron at compatible recorded versions, stop and amend the durable-operation decision rather than introduce a second queue or detached background workaround.
- (assumption) Project-seeding code tracing identifies a valid PROMPT composition seam, but executable fixture proof of exact business name, selected facts, omitted unknowns, and editability remains required before Stories 4.2 onward.
- (direction) Provider choices for interactive discovery, weak-site policy, WhatsApp/BSP, activation send outcome, and retention remain story-gated ODs 4 through 9; this spine fixes their ports and authority boundaries without selecting vendors or commercial policy.
- (direction) OD-14 remains deliberately deferred: measure representative discovery, qualification, AI/project, sandbox, deployment, storage/egress, outreach, retry, latency, and concurrency costs before Andrew approves any Jagwar plan, price, allowance, top-up, trial gate, or enforcement.
- (event) Architecture spine distilled from the memlog into ARCHITECTURE-SPINE.md.
- (event) Deterministic lint_spine.py completed with zero findings.
- (event) Reviewer gate dispatched: rubric walker, technology currency, adversarial divergence, security/data integrity, and load-bearing input reconciliation.
- (decision by architect) Reviewer reconciliation: dispatch uses a recipient-channel lock and immediate authoritative consent/suppression/publication/entitlement revalidation held through provider outcome transition.
- (decision by architect) Reviewer reconciliation: same-owner composite keys/FKs plus repository owner predicates and a restricted worker role are mandatory.
- (decision by architect) Reviewer reconciliation: operation admission hashes inputs; PGMQ delivery is at-least-once; closed operation/attempt states, fencing, terminal-before-archive, callback inbox, and unknown-outcome reconciliation are specified.
- (decision by architect) Reviewer reconciliation: canonical CandidateV1 Zod contract has explicit DB mappers/round-trip tests; facts use verified-or-unknown union; raw provider payload retention is excluded.
- (decision by architect) Reviewer reconciliation: Cron-to-HTTP requires pg_cron, pg_net, Vault, signed replay-resistant requests, least privilege, and measured runtime/batch/lease preflight.
- (decision by architect) Reviewer reconciliation: cost_observation is physically separate from billing usage_records with phase uniqueness and estimate-to-actual supersession.
- (decision by architect) Reviewer reconciliation: donor migration is offline dry-run-first and cannot be a runtime dependency or cause external effects.
- (decision by architect) Reviewer reconciliation: commercial UI preserves Onlook primitives and must meet WCAG 2.2 AA keyboard, focus, announcement, motion, responsive, and non-color invariants.
- (gap by architect) OD-13 operator/admin surface remains unresolved and blocking because OD-15 source is unavailable; adminProcedure is not treated as role authorization.
- (event by architect) Architecture reviewer findings applied comprehensively; mechanical spine lint passed with zero findings.
- (decision by Andrew) OD-15 resolved on 2026-07-28 through approved CCR-019 through CCR-022: Jagwar intentionally removes the unavailable private upstream apps/admin registration, gitlink, root dev script, and generated lock records and does not claim private-admin parity.
- (evidence) Pinned Bun 1.3.1 frozen install succeeds with 2,326 packages and the existing @onlook/web-client typecheck passes after the OD-15 target resolution.
- (constraint) OD-13 remains the independent blocker for explicit operator role, authorization, target-native surface placement, protected paths, and regression boundaries; adminProcedure is not operator authorization.
